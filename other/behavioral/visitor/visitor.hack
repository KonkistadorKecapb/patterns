namespace Visitor;

<<__EntryPoint>>
function visitor(): void {

    echo "---=== 1) visitor ===---\n";
    echo
        "Cannot be implemented on Hack since do not support method overloading. Or I'm wrong? no need overloading?\n";
}
